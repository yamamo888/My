# -*- coding: utf-8 -*-

import os
import shutil

import numpy as np


# Path ------------------------------------------------------------------------
logsPath = "logs"
dirPath = "b2b3b4b5b6400-450"
mvPath = "tmp"
# -----------------------------------------------------------------------------

# Reading ---------------------------------------------------------------------
pathFile = np.loadtxt("Top_1_path.txt").split("/")[-1]
# -----------------------------------------------------------------------------

# Move ------------------------------------------------------------------------    
for file in pathFile:
    if os.path.exists(file):
        shutil.move(file,os.path.join(logsPath,mvPath,file))
# -----------------------------------------------------------------------------
